# Test Cases — R1-05 HR: Payroll (รอบเงินเดือน)

| folder | jira | title | precondition | steps | expected_result | squad | priority | automation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| R1-05 | | View payroll run list | ผู้ใช้ login ด้วย role `hr_admin` หรือ `finance_manager` | 1. เข้าเมนู HR → Payroll<br>2. รอระบบโหลด | แสดงรายการ payroll run พร้อม status (`draft`, `processed`, `approved`, `paid`) และข้อมูลสรุป | | High | |
| R1-05 | | Filter payroll runs by status | ผู้ใช้อยู่ที่หน้า Payroll | 1. เลือก filter status เช่น `processed`<br>2. รอผลกรอง | แสดงเฉพาะ payroll run ที่มีสถานะตรงกัน | | Medium | |
| R1-05 | | Create new payroll run | ผู้ใช้ login ด้วย role `hr_admin` | 1. คลิกปุ่ม "Create Payroll Run"<br>2. กรอก period (เดือน/ปี)<br>3. กรอก payDate<br>4. คลิก "Save" | สร้าง payroll run ใหม่สำเร็จ สถานะเป็น `draft` | | High | |
| R1-05 | | Create payroll run fails with missing period | ผู้ใช้กำลังสร้าง payroll run | 1. เปิดฟอร์มสร้าง payroll run<br>2. ไม่กรอก period หรือ payDate<br>3. คลิก "Save" | แสดง validation error ระบุฟิลด์ที่ต้องกรอก | | Medium | |
| R1-05 | | Process payroll run | ผู้ใช้มี payroll run สถานะ `draft` | 1. เปิด payroll run ที่มีสถานะ `draft`<br>2. คลิกปุ่ม "Process"<br>3. ยืนยันการ process | ระบบคำนวณ payslips, SS, WHT, deductions จาก unpaid leave และเปลี่ยนสถานะเป็น `processed` | | High | |
| R1-05 | | Process payroll fails when config is missing | ผู้ใช้พยายาม process payroll แต่ config ไม่ครบ | 1. เปิด payroll run สถานะ `draft`<br>2. คลิก "Process"<br>3. สังเกตผลลัพธ์ | ระบบ block การ process และแสดงข้อความว่า config (เช่น tax settings) ไม่ครบ | | High | |
| R1-05 | | Approve payroll run | ผู้ใช้มี payroll run สถานะ `processed` | 1. เปิด payroll run ที่มีสถานะ `processed`<br>2. ตรวจสอบ summary และ payslips<br>3. คลิกปุ่ม "Approve"<br>4. ยืนยัน | Payroll run เปลี่ยนสถานะเป็น `approved` พร้อมจ่ายจริง | | High | |
| R1-05 | | Cannot approve run that is not processed | ผู้ใช้พยายาม approve payroll run ที่ยังเป็น `draft` | 1. เปิด payroll run สถานะ `draft`<br>2. ตรวจสอบปุ่ม Approve | ปุ่ม "Approve" ถูกซ่อนหรือ disable ไม่สามารถ approve run ที่ยังไม่ process | | High | |
| R1-05 | | Mark payroll run as paid | ผู้ใช้มี payroll run สถานะ `approved` | 1. เปิด payroll run ที่มีสถานะ `approved`<br>2. คลิกปุ่ม "Mark Paid"<br>3. กรอก paymentReference<br>4. ยืนยัน | Payroll run เปลี่ยนสถานะเป็น `paid` และ trigger integration ส่งข้อมูลไป Finance | | High | |
| R1-05 | | Finance integration triggered after mark paid | ผู้ใช้ mark payroll run เป็น paid สำเร็จ | 1. Mark payroll run เป็น paid<br>2. ตรวจสอบว่ามี journal entry สำหรับ payroll ถูกสร้างใน Finance | ระบบสร้าง journal entry ใน Finance Module สำหรับ payroll ที่จ่ายแล้ว | | High | |
| R1-05 | | View payslips for a payroll run | ผู้ใช้มี payroll run ที่ผ่าน process แล้ว | 1. เปิด payroll run ที่มีสถานะ `processed` หรือสูงกว่า<br>2. เลือกดู payslips<br>3. รอรายการโหลด | แสดงรายการ payslip รายคน พร้อมยอดเงินเดือน ค่าลดหย่อน และยอดสุทธิ | | High | |
| R1-05 | | Export payslip for an employee | ผู้ใช้อยู่ที่หน้ารายการ payslip | 1. เลือก payslip ของพนักงานคนหนึ่ง<br>2. คลิกปุ่ม Export หรือ Download<br>3. รอไฟล์ดาวน์โหลด | ดาวน์โหลด payslip เป็นไฟล์ PDF ได้สำเร็จ | | Medium | |
| R1-05 | | View payroll config settings | ผู้ใช้ login ด้วย role `hr_admin` | 1. เข้าหน้า Payroll<br>2. คลิกลิงก์ Settings หรือ Config<br>3. รอหน้าโหลด | แสดงการตั้งค่า payroll configs, allowance types, tax settings | | Medium | |
| R1-05 | | Save payroll config | ผู้ใช้อยู่ที่หน้า Payroll Settings | 1. แก้ไขค่า config ที่ต้องการ<br>2. คลิก "Save"<br>3. รอการบันทึก | ค่า config ถูกบันทึกสำเร็จ ระบบจะใช้ค่าใหม่ในการคำนวณครั้งถัดไป | | High | |
| R1-05 | | Unpaid leave deduction reflected in payslip | พนักงานมีการลาที่ approved เป็น unpaid leave ในรอบเดียวกัน | 1. Process payroll run ที่ครอบคลุมช่วงที่มี unpaid leave<br>2. ดู payslip ของพนักงานที่ลาแบบ unpaid | Payslip แสดงรายการหักค่าจ้างสำหรับวันลาที่ไม่ได้รับค่าจ้าง | | High | |
| R1-05 | | Cannot delete approved or paid payroll run | ผู้ใช้พยายามลบ payroll run ที่ไม่ใช่ draft | 1. เปิด payroll run ที่มีสถานะ `approved` หรือ `paid`<br>2. ตรวจสอบว่ามีปุ่ม Delete หรือไม่ | ปุ่ม Delete ถูกซ่อนหรือ disable สำหรับ run ที่ผ่าน draft แล้ว | | High | |
