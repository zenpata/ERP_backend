# Test Cases — R1-01 Auth: Login, Session, และเปลี่ยนรหัสผ่าน

| folder | jira | title | precondition | steps | expected_result | squad | priority | automation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| R1-01 | | Successful login with valid credentials | ผู้ใช้อยู่ที่หน้า `/login` และมีบัญชีที่ active ในระบบ | 1. กรอกอีเมลที่ถูกต้องในช่อง email<br>2. กรอกรหัสผ่านที่ถูกต้องในช่อง password<br>3. คลิกปุ่ม "เข้าสู่ระบบ" | ผู้ใช้เข้าสู่ระบบสำเร็จ และถูกนำไปยังหน้า landing ตาม role/permission ที่กำหนด | | High | |
| R1-01 | | Failed login with wrong password | ผู้ใช้อยู่ที่หน้า `/login` | 1. กรอกอีเมลที่มีในระบบ<br>2. กรอกรหัสผ่านที่ไม่ถูกต้อง<br>3. คลิกปุ่ม "เข้าสู่ระบบ" | แสดงข้อความแสดงข้อผิดพลาดทั่วไป ไม่เปิดเผยว่าอีเมลมีในระบบหรือไม่ ไม่เข้าสู่ระบบ | | High | |
| R1-01 | | Failed login with unregistered email | ผู้ใช้อยู่ที่หน้า `/login` | 1. กรอกอีเมลที่ไม่มีในระบบ<br>2. กรอกรหัสผ่านใด ๆ<br>3. คลิกปุ่ม "เข้าสู่ระบบ" | แสดงข้อความแสดงข้อผิดพลาดทั่วไป ไม่เปิดเผยว่าอีเมลมีในระบบหรือไม่ | | High | |
| R1-01 | | Failed login with inactive account | ผู้ใช้มีบัญชีที่ถูก deactivate ในระบบ | 1. กรอกอีเมลของบัญชีที่ inactive<br>2. กรอกรหัสผ่านที่ถูกต้อง<br>3. คลิกปุ่ม "เข้าสู่ระบบ" | แสดงข้อความแสดงข้อผิดพลาด ไม่อนุญาตให้เข้าสู่ระบบ | | High | |
| R1-01 | | Login form submit button disabled when fields empty | ผู้ใช้อยู่ที่หน้า `/login` ยังไม่ได้กรอกข้อมูลใด | 1. เปิดหน้า `/login`<br>2. ไม่กรอกข้อมูลใด ๆ<br>3. ตรวจสอบสถานะปุ่ม "เข้าสู่ระบบ" | ปุ่ม "เข้าสู่ระบบ" ถูก disable ไม่สามารถคลิกได้ | | Medium | |
| R1-01 | | Login with Enter key press | ผู้ใช้อยู่ที่หน้า `/login` | 1. กรอกอีเมลที่ถูกต้อง<br>2. กรอกรหัสผ่านที่ถูกต้อง<br>3. กด Enter บนแป้นพิมพ์ | ระบบ submit form และเข้าสู่ระบบสำเร็จเช่นเดียวกับการคลิกปุ่ม | | Medium | |
| R1-01 | | Show/hide password toggle | ผู้ใช้อยู่ที่หน้า `/login` | 1. กรอกรหัสผ่านในช่อง password<br>2. คลิกปุ่ม Show/Hide Password<br>3. คลิกอีกครั้งเพื่อซ่อน | รหัสผ่านสลับระหว่างแสดงและซ่อน โดยไม่ทำการ submit form | | Low | |
| R1-01 | | Redirect to login with expired session banner | ผู้ใช้ถูก redirect มาจากหน้าที่ session หมดอายุ (`?reason=expired`) | 1. เปิด URL `/login?reason=expired`<br>2. ตรวจสอบ UI | แสดง banner หรือข้อความแจ้งว่า session หมดอายุ ให้ผู้ใช้ทราบก่อน login ใหม่ | | Medium | |
| R1-01 | | Session bootstrap success after login | ผู้ใช้เพิ่งกรอก credential ถูกต้องและระบบได้รับ token แล้ว | 1. Login ด้วย credential ที่ถูกต้อง<br>2. สังเกตกระบวนการโหลดหลัง login<br>3. ตรวจสอบว่าหน้า app shell โหลดขึ้นมา | ระบบเรียก `GET /api/auth/me` สำเร็จ โหลด profile และ permission ครบ นำผู้ใช้ไปหน้า landing ตาม role | | High | |
| R1-01 | | Bootstrap failure redirects back to login | ผู้ใช้ login สำเร็จแล้ว แต่ `GET /api/auth/me` ล้มเหลว | 1. Login ด้วย credential ที่ถูกต้อง<br>2. จำลอง `/api/auth/me` ตอบ 401 หรือ 500<br>3. สังเกตผลลัพธ์ | ระบบ clear session และนำผู้ใช้กลับหน้า login พร้อมข้อความแจ้งให้ติดต่อ admin | | High | |
| R1-01 | | Silent refresh on expired access token | ผู้ใช้ login อยู่และ access token กำลังจะหมดอายุระหว่างใช้งาน | 1. ใช้งานระบบ (กด action หรือโหลดหน้า)<br>2. Access token หมดอายุ<br>3. สังเกตว่าระบบทำอะไร | ระบบทำ silent refresh โดยเรียก `POST /api/auth/refresh` โดยผู้ใช้ไม่รู้สึกถูกรบกวน action เดิมสำเร็จต่อเนื่อง | | High | |
| R1-01 | | Redirect to login when refresh token expires | ผู้ใช้ login อยู่ และทั้ง access token และ refresh token หมดอายุ | 1. Access token หมดอายุ<br>2. ระบบพยายาม refresh แต่ refresh token ก็หมดอายุหรือถูก revoke<br>3. สังเกตผลลัพธ์ | ระบบ clear session และ redirect ผู้ใช้ไป `/login?reason=expired` | | High | |
| R1-01 | | Logout with confirmation | ผู้ใช้ login อยู่ในระบบ | 1. คลิกเมนู Account หรือ Logout<br>2. ยืนยันการ logout ใน modal ที่แสดง<br>3. สังเกตผลลัพธ์ | ระบบเรียก `POST /api/auth/logout` เคลียร์ session ฝั่ง client และนำผู้ใช้กลับหน้า `/login` | | High | |
| R1-01 | | Cancel logout stays on current page | ผู้ใช้ login อยู่ในระบบ | 1. คลิกเมนู Logout<br>2. คลิก "Cancel" ใน modal ยืนยัน | Modal ปิดลง ผู้ใช้ยังคงอยู่ในหน้าเดิม session ยังคงใช้งานได้ | | Medium | |
| R1-01 | | Logout clears session even when API fails | ผู้ใช้ login อยู่ในระบบ | 1. จำลอง network error สำหรับ `/api/auth/logout`<br>2. คลิก Logout และยืนยัน<br>3. สังเกตว่า session ฝั่ง client ถูก clear หรือไม่ | แม้ API จะล้มเหลว ระบบยังคง clear session ฝั่ง client และนำผู้ใช้กลับหน้า login (security-first) | | High | |
| R1-01 | | Change password successfully | ผู้ใช้ login อยู่ และเปิดหน้า Settings / Security tab | 1. กรอกรหัสผ่านปัจจุบันที่ถูกต้อง<br>2. กรอกรหัสผ่านใหม่ที่ผ่าน policy (อย่างน้อย 8 ตัว)<br>3. กรอกยืนยันรหัสผ่านใหม่ให้ตรงกัน<br>4. คลิก "บันทึก" | ระบบเรียก `PATCH /api/auth/me/password` สำเร็จ แสดง toast สำเร็จ ฟอร์มถูก clear | | High | |
| R1-01 | | Change password fails with wrong current password | ผู้ใช้ login อยู่ และเปิดหน้า Security tab | 1. กรอกรหัสผ่านปัจจุบันที่ไม่ถูกต้อง<br>2. กรอกรหัสผ่านใหม่<br>3. กรอกยืนยันรหัสผ่านใหม่<br>4. คลิก "บันทึก" | แสดง inline error ว่ารหัสผ่านปัจจุบันไม่ถูกต้อง ฟอร์มยังคงอยู่เพื่อให้แก้ไข | | High | |
| R1-01 | | Change password fails when new passwords don't match | ผู้ใช้ login อยู่ และเปิดหน้า Security tab | 1. กรอกรหัสผ่านปัจจุบันที่ถูกต้อง<br>2. กรอกรหัสผ่านใหม่<br>3. กรอกยืนยันรหัสผ่านใหม่ที่ไม่ตรงกัน<br>4. คลิก "บันทึก" | แสดง validation error ว่า confirm password ไม่ตรงกับ new password ระบบไม่ submit | | High | |
| R1-01 | | Change password fails with weak new password | ผู้ใช้ login อยู่ และเปิดหน้า Security tab | 1. กรอกรหัสผ่านปัจจุบันที่ถูกต้อง<br>2. กรอกรหัสผ่านใหม่ที่สั้นกว่า 8 ตัว<br>3. กรอกยืนยันรหัสผ่านใหม่<br>4. คลิก "บันทึก" | แสดง validation error เรื่อง password policy ไม่ผ่าน | | Medium | |
| R1-01 | | Menu items visible only for authorized roles | ผู้ใช้ login ด้วย role ที่มีสิทธิ์จำกัด (เช่น employee) | 1. Login ด้วย role `employee`<br>2. ตรวจสอบ sidebar/menu ที่แสดง | เมนูที่แสดงตรงกับ permission ของ role นั้น ไม่แสดงเมนูที่ไม่มีสิทธิ์เข้าถึง | | High | |
| R1-01 | | Access denied for unauthorized route | ผู้ใช้ login ด้วย role ที่ไม่มีสิทธิ์เข้าถึง route นั้น | 1. Login ด้วย role ที่ไม่มีสิทธิ์<br>2. พยายามเข้า URL ที่ไม่มีสิทธิ์โดยตรง (เช่น `/settings/users`) | แสดงหน้า access denied หรือ redirect ไปหน้าที่เหมาะสม ไม่แสดงข้อมูลที่ไม่มีสิทธิ์ | | High | |
| R1-01 | | Account locked after 5 consecutive failed logins | ผู้ใช้มีบัญชีที่ active ในระบบ | 1. กรอก email ที่ถูกต้อง<br>2. กรอกรหัสผ่านที่ไม่ถูกต้องซ้ำ 5 ครั้งติดกัน<br>3. สังเกตข้อความหลังครั้งที่ 5 | ระบบแสดงข้อความว่าบัญชีถูกล็อก และแนะนำให้ติดต่อ Admin เพื่อ unlock ไม่สามารถ login ได้จนกว่า Admin จะ unlock | | High | |
| R1-01 | | Locked account cannot login even with correct password | ผู้ใช้มีบัญชีที่ถูกล็อกจากการ login ผิดพลาด | 1. กรอก email และรหัสผ่านที่ถูกต้อง<br>2. คลิกปุ่ม "เข้าสู่ระบบ" | ระบบปฏิเสธการ login แสดงข้อความว่าบัญชีถูกล็อก แม้รหัสผ่านจะถูกต้อง | | High | |
