// Monthly depreciation cron — disabled until the 'cron' dep is added.
// Business logic lives in AssetsService.runMonthlyDepreciation; wire it up
// when a scheduler is introduced.
export const monthlyDepreciationCron = null
